version: "2"
sql:
  - engine: "sqlite"
    queries: "./sql-queries/**"
    schema: "./schema.sql"
    gen:
      go:
        package: "sqlcDb"
        out: "sqlcDb"
        sql_package: "database/sql"
        emit_json_tags: true
